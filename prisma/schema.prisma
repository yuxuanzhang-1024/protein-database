datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Protein {
  id        String    @id @default(auto()) @map("_id") @db.ObjectId
  ID        String
  GLN       Float    @db.Double
  pLDDT     Float    @db.Double
  BSA       Float    @db.Double
  Core_GLN  Float    @db.Double@map("Core GLN")
  Symmetry  String
  Proteome  String
  File_Url  String
  Image_Url String   
}